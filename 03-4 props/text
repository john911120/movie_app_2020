props : 리액트 내부에서 컴포넌트에서 컴포넌트로 전달하는 데이터를 의미한다.

함수의 매개변수 개념과 같으며 props를 통해 효율적으로 재사용을 할 수 있다.

컴포넌트에 props를 전달하면 props에 있는 데이터가 하나의 객체로 변환되어 인자가 전달되고 
전달받은 인자를 함수에서 사용할 수 있다.
추가로 ES6의 구조 분해 할당을 사용하면 PROPS를 더 편리한 방법으로 사용할 수 있다.
